//Уровень 1.1
/*Дано число. Проверьте, отрицательное оно или нет.
Выведите об этом информацию в консоль*/

/*let input = prompt("Введите число:"); // Получаем строку
let number = parseFloat(input);       // Преобразуем в число (дробное или целое)

if (isNaN(number)) {
    console.log("Это не число!");
} else if (number < 0) {
    console.log(`Число ${number} отрицательное`);
} else {
    console.log(`Число ${number} не отрицательное`);
}*/


/*Дана строка. Выведите в консоль длину этой строки*/

/*str = prompt();
console.log(`длина строки: ${str.length}`);*/

/*Дана строка. Выведите в консоль последний символ строки*/
//1
/*str2 = prompt();
console.log(str2[str2.length - 1]);*/ 

//2
const str = prompt();
console.log(str.slice(-1)); 